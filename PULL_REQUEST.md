# Title: feat: 栄冠ナイン(3年縛り)攻略Wikiの追加（詳細データ・画像ローカル化含む）

## 変更概要

既存のクイズアプリに、新たに「パワプロ2026-2027 栄冠ナイン(3年縛り)攻略Wiki」を追加しました。

### 主な変更点
1. **Wikiセクションの追加**: Astro（静的サイトジェネレーター）で構築した全10カテゴリの攻略ページ
2. **Flaskルートの追加**: `app.py` に `/wiki/` パス以下のルーティングを追加（既存ルートへの変更なし）
3. **配信方式**: ビルド済み静的ファイル(`wiki/dist`)をFlask経由で配信。本番環境でNode.js不要
4. **詳細データ追記**: 複数攻略サイトを突き合わせた裏取り済みデータ（戦術レベル計算式、固有戦術効果、経験値テーブル、都道府県別OB一覧等）を全ページに統合
5. **画像のローカル化**: 外部サイト(Game8等)への画像直リンクをすべて解消し、`wiki/public/images/` にローカル保存・参照に変更

## 変更ファイル一覧・規模

| カテゴリ | ファイル数 | 追加行数 |
|---|---|---|
| `app.py` (既存ファイル変更) | 1 | +40行（末尾にWikiルート追加のみ） |
| `wiki/src/` (Astroソース) | ~20 | ~1,300行 |
| `wiki/dist/` (ビルド済みHTML) | ~55 | ~3,800行 |
| `wiki/public/images/` (ローカル画像) | ~30 | バイナリ(合計~250KB) |
| `wiki/` (設定・README等) | ~10 | ~4,500行(大半はpackage-lock.json) |
| `build.sh` (ビルドスクリプト) | 1 | +29行 |
| **合計** | **106ファイル** | **+8,869行 / -1行** |

## 実施した動作確認内容

### ビルド確認
- `cd wiki && npm run build` を実行し、エラーなく全11ページがビルドされることを確認
- ビルド後のHTML内のすべての `<img>` タグについて、参照先ファイルが存在することをPythonスクリプトで全件検証（リンク切れ: 0件）

### ルーティング確認
- Flaskを起動し `/wiki/` 以下の全ルートにアクセスし正しくページが表示されることを確認
- 各カテゴリページの内部リンクが404にならないことを確認

### セキュリティ確認
- リポジトリ全体に対し、APIキー・シークレット・個人情報のパターン検索を実施
  - `secret|password|api_key|token|CLIENT_ID|CLIENT_SECRET` → 検出なし
  - `AIza|ghp_|gho_|sk-|Bearer|PRIVATE_KEY|-----BEGIN` → 検出なし
  - 個人情報(メールアドレス等) → 検出なし（ドメイン名 `das-mymt.com` はDEPLOY_CHECKLISTのURL内のみ）

## 既存機能への影響について

### 影響なし（安全）の根拠

1. **ルーティングの完全分離**
   - `main` ブランチの既存ルート: `/`, `/login`, `/auth/callback`, `/logout`, `/api/upload`, `/api/files`, `/api/files/public`, `/api/files/<file_id>/save`, `/api/files/<file_id>`, `/api/files/<file_id>/groups`, `/api/quiz`, `/api/pdf` (計12ルート)
   - 追加ルート: `/wiki/`, `/wiki/<path:filename>` (計2ルート)
   - パスの衝突は一切なし

2. **`login_required` デコレータへの影響なし**
   - Wikiルートには `@login_required` を**意図的に付与していない**（仕様通り非ログインで閲覧可能）
   - 既存ルートの `@login_required` の定義・適用箇所に変更なし

3. **SQLiteテーブル定義への影響なし**
   - `init_db()` 関数および4テーブル（`users`, `csvfiles`, `questions`, `saved_files`）の定義に変更なし
   - Wikiルートは一切DBアクセスを行わない（静的ファイル配信のみ）

4. **既存テストへの影響**
   - リポジトリにpytestの設定ファイルやテストディレクトリが存在しないため、テストの実行・破壊はなし

5. **import文の変更**
   - `from flask import ...` に `send_from_directory, abort` を追加（Flask標準モジュール、副作用なし）
   - `import logging` を追加（Python標準ライブラリ、副作用なし）

## レビュー時の確認ポイント

- `app.py` の差分はファイル末尾の40行のみ（Wikiルート追加）。1行目のimport文に2語追加
- `wiki/dist/` はビルド済みHTMLのため、マージ後にラズパイ側でNode.jsのインストールやビルドは不要
- 画像はすべてローカル化済み。外部サイトへの依存なし

## スクリーンショット
（※PR作成時に、Wikiトップページおよびカテゴリページのスクリーンショットを添付してください）
